from pathlib import Path

#alll the paths
CONFIG_FILE_PATH=Path("config/config.yaml")
PARAMS_FILE_PATH=Path("params.yaml")
SCHEMA_FILE_PATH=Path("schema.yaml")

# from pathlib import Path

# # Automatically get project root
# PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent  # adjust depending on file depth

# CONFIG_FILE_PATH = PROJECT_ROOT / "config" / "config.yaml"
# PARAMS_FILE_PATH = PROJECT_ROOT / "config" / "params.yaml"
# SCHEMA_FILE_PATH = PROJECT_ROOT / "config" / "schema.yaml"
